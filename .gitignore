# MacOS DS_Store files
.DS_Store

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Snapshot test diff artifacts (written by assertGolden on failure)
*.golden.got

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
coverage.html
coverage-e2e-raw/

# Dependency directories (remove the comment below to include it)
#vendor/

# Built binaries (go build without -o drops them in the project root)
/kanban-md

# Output of GoReleaser
dist/

# Visual Studio Code files
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# Local History for Visual Studio Code
.history/

# GoLand and IntelliJ IDEA files
.idea/

# env files that usually contain secrets or local config
.env
.envrc

# Local kanban board (used for dogfooding during development)
/kanban/

# Internal docs (plans, research reports) — not shipped
/docs/
